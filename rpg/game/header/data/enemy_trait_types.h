#pragma once

enum class enemy_aggression {
    aggressive,
    neutral,
    passive
};

enum class enemy_battle_style {
    direct,
    coward,
    thief
};

enum class enemy_family {
    human,
    insect,
    canine,
    hooved,
    winged,
    shelled,
    reptile,
    unique
};

enum class enemy_social {
    lone,
    flock,
    collective
};
